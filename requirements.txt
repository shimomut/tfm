# Python 3.10+ required (PuiKit, TFM's UI framework, requires 3.10+)
# pygments for syntax highlighting (will fallback gracefully if not available)
# boto3 for AWS S3 support
# watchdog for automatic file list reloading
# pillow for the built-in image viewer (decode/crop/scale; the viewer degrades
#   to a metadata card without it, and inline terminal images need it too)
#
# Platform-specific dependencies are selected automatically via environment
# markers below, so this file is the single source of truth for both
# `make venv` (pip install -r requirements.txt) and setup.py install_requires:
#   - pyobjc         : macOS desktop mode (CoreGraphics backend)
#   - windows-curses : curses support on Windows

pygments
boto3
watchdog
pillow
pyobjc; sys_platform == "darwin"
windows-curses; sys_platform == "win32"
